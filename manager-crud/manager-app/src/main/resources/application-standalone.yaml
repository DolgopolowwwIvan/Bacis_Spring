ivdev:
  services:
    catalogue:
      uri: http://localhost:8081
      registration-id: keycloak
spring:
  security:
    oauth2:
      client:
        registration:
          keycloak:
            client-id: manager-app
            client-secret: YJcW473HVszYF2tBCY915UdePufQipMK
            scope:
              - openid
              - view_catalogue
              - edit_catalogue
              - microprofile-jwt
        provider:
          keycloak:
            issuer-uri: http://localhost:8181/realms/manager-crud
            user-name-attribute: preferred_username